# This file has been cleaned up post-releasing version 0.14.1.
# Generate its content quickly using the following command:
#   git log origin..HEAD | grep "^\s" > .semrelease/this_release
#!VERSION=0.14.2
- patch(build-portfolio): minor prompt change.
- patch(review-portfolio): simplify prompt to build and execute rebalance plan.
